// Note: v-owl-carousel is not compatible with Vue 3/Nuxt 3
// This plugin needs to be replaced with a Vue 3 compatible carousel solution
// For now, we'll use Swiper which is already included in dependencies

export default defineNuxtPlugin(() => {
  // TODO: Replace with Vue 3 compatible carousel
  // Consider using Swiper Vue components instead
})
